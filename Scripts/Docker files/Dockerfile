# Use the official lightweight Nginx image
FROM nginx:alpine

# Copy all static website files from your local directory to the Nginx server directory
COPY . /usr/share/nginx/html

# Expose port 80 to allow web traffic
EXPOSE 80